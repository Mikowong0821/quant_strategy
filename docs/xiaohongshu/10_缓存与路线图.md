# 10｜缓存、落盘与路线图：能复现，才敢迭代

**发笔记用标题（可改）：**  
量化笔记｜为什么我要把中间结果也存进 `output`？

---

**正文**

`live/cache_io` 可选把行情、面板、IC、配置快照、绩效汇总、调仓日志等写成 CSV / JSON：不是为了占磁盘，是为了 **对照**：「上周那版参数跑出来的 IC 均值是不是这一版」。

`persist_run_outputs` 打开后，PNG、`performance_summary.csv`、`rebalance_logs/*.csv`、`run_config.json` 都会落在 `config` 指定的输出目录；其中 `nav_compare` 和 `excess_nav_compare` 可以分别看绝对净值与相对基准表现，适合直接当小红书配图或写公号的素材，也适合回看一次实验的完整条件。

目录里 **`live` 下的信号、模拟盘** 目前是 **占位**：MVP 先把研究链路灯跑绿，再接「可执行指令 → 仿真成交 → 真实报单」。顺序反过来，排错成本通常是指数级。

**本篇讲清的量化核心点：**  
可复现性；**研究 / 仿真 / 实盘** 分阶段交付，而不是一锅炖。

---

**配图建议**

- 文件夹截图：`output/` 下一组 `nav_compare`、`excess_nav_compare`、`performance_summary.csv`、`rebalance_logs/`、`ic_*`、`weights_*`（打码路径）。  
- 路线图字卡：MVP → 信号 → 仿真 → 实盘，四格漫画风。

---

**话题标签（示例）**

#量化工程 #可复现研究 #模拟盘 #实盘 #程序员 #量化成长路线
